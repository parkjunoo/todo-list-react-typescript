import styled from 'styled-components';

const HeaderWrapper = styled.div`
  width: 100%;
  font-size: 30px;
  margin-top: 50px;
  display: block;
  text-align: center;
`;

export default { HeaderWrapper };
